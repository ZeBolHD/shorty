import LinkCard from "@/components/LinkCard";

const Home = () => {
  return (
    <main className="w-full min-h-screen flex items-center justify-center max-mobile:px-4">
      <LinkCard />
    </main>
  );
};

export default Home;
