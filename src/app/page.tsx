import LinkCard from "@/components/LinkCard";

const Home = () => {
  return (
    <main className="w-full min-h-full flex items-center justify-center max-mobile:px-4 overflow-hidden">
      <LinkCard />
    </main>
  );
};

export default Home;
